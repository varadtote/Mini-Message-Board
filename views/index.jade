extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href='/new') Add New Message
  ul
    each val in messages
      li
        div.messageCard
          h3 #{val.user}
          p #{val.text}
          p.dateText #{val.added} 


